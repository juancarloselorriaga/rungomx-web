# Database connection string for Neon/Postgres with Drizzle
DATABASE_URL="postgres://user:password@host:port/dbname"

# Better Auth - Secret for signing and encrypting tokens (generate with: openssl rand -base64 32)
BETTER_AUTH_SECRET="your-secret-key-replace-with-random-string"

# Google OAuth credentials for Better Auth
GOOGLE_CLIENT_ID="your-google-oauth-client-id"
GOOGLE_CLIENT_SECRET="your-google-oauth-client-secret"

# Brevo (formerly SendinBlue) - Email service for transactional emails
# Get your API key from: https://app.brevo.com/settings/keys/api
BREVO_API_KEY="your-brevo-api-key"
BREVO_SENDER_EMAIL="noreply@yourdomain.com"
BREVO_SENDER_NAME="RungoMX"
# Comma-separated list of support email recipients for contact/feedback forms
BREVO_SUPPORT_RECIPIENTS="support@example.com"

# Mapbox - Get your access token from: https://account.mapbox.com/access-tokens/
MAPBOX_ACCESS_TOKEN="your-mapbox-access-token"
NEXT_PUBLIC_MAP_STYLE_LIGHT=https://api.mapbox.com/styles/v1/your-user/your-light-style-id?access_token=YOUR_MAPBOX_PUBLIC_TOKEN
NEXT_PUBLIC_MAP_STYLE_DARK=https://api.mapbox.com/styles/v1/your-user/your-dark-style-id?access_token=YOUR_MAPBOX_PUBLIC_TOKEN

# Vercel Blob - Storage for profile pictures
# Get your token from: Vercel Dashboard → Storage → Create Blob Store → then copy the token
BLOB_READ_WRITE_TOKEN="vercel_blob_rw_xxxxxxxxxxxx"

# Optional: additional trusted origins for Better Auth (comma-separated, server-side only)
AUTH_ADDITIONAL_TRUSTED_ORIGINS=""

# Optional: additional protection for cron endpoints.
# If set, you can manually trigger cron routes by sending:
# Authorization: Bearer $CRON_SECRET
CRON_SECRET=""

# Events platform no-payment mode (auto-confirms registrations)
NEXT_PUBLIC_FEATURE_EVENTS_NO_PAYMENT_MODE=false

# Events platform registration TTLs (server-side only)
EVENTS_REGISTRATION_STARTED_TTL_MINUTES=30
EVENTS_REGISTRATION_SUBMITTED_TTL_MINUTES=30
EVENTS_REGISTRATION_PAYMENT_PENDING_TTL_HOURS=24

# Events platform group registrations upload rate limit (server-side only)
# Applies per authenticated user for the "group_registration_upload" action.
EVENTS_GROUP_REGISTRATION_UPLOAD_MAX_REQUESTS=6
EVENTS_GROUP_REGISTRATION_UPLOAD_WINDOW_MS=600000

# Events platform group upload (invite/claim) settings (server-side only)
# Optional: invite token secret (falls back to AUTH_SECRET/NEXTAUTH_SECRET if unset)
EVENTS_INVITE_TOKEN_SECRET=""
EVENTS_GROUP_UPLOAD_MAX_ROWS=250
EVENTS_GROUP_UPLOAD_RESERVE_CHUNK_SIZE=25
EVENTS_GROUP_UPLOAD_INVITE_SEND_CHUNK_SIZE=10
EVENTS_GROUP_UPLOAD_CREATE_BATCH_MAX_REQUESTS=5
EVENTS_GROUP_UPLOAD_CREATE_BATCH_WINDOW_MS=600000
EVENTS_GROUP_UPLOAD_INVITE_MAX_SEND_COUNT=5
EVENTS_GROUP_UPLOAD_INVITE_RESEND_COOLDOWN_MS=600000
EVENTS_GROUP_UPLOAD_INVITE_SEND_RATE_WINDOW_MS=5000
