export function eventEditionDetailTag(editionId: string) {
  return `event-edition:${editionId}:detail`;
}

export function eventEditionAddOnsTag(editionId: string) {
  return `event-edition:${editionId}:add-ons`;
}

export function eventEditionCouponsTag(editionId: string) {
  return `event-edition:${editionId}:coupons`;
}

export function eventEditionQuestionsTag(editionId: string) {
  return `event-edition:${editionId}:questions`;
}

export function eventEditionPricingTag(editionId: string) {
  return `event-edition:${editionId}:pricing`;
}

export function eventEditionWebsiteTag(editionId: string) {
  return `event-edition:${editionId}:website`;
}

export function eventEditionRegistrationsTag(editionId: string) {
  return `event-edition:${editionId}:registrations`;
}

